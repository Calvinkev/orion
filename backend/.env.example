# ORION Music Promotion Platform - Environment Variables Template
# Copy this to .env and fill in your values

# ==== FOR RAILWAY DEPLOYMENT ====
# Railway provides these automatically when you add a MySQL database:
# - MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE
# - Or use MYSQL_URL for connection string

# ==== FOR LOCAL DEVELOPMENT ====
# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=orion_db

# JWT Secret (generate a secure random string for production)
JWT_SECRET=your_secure_jwt_secret_key_here

# Server Port (Railway sets PORT automatically)
PORT=5000

# Environment (set to 'production' on Railway)
NODE_ENV=development

# Upload directories
UPLOAD_DIR=uploads
FRONTEND_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
